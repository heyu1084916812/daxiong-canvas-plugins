"""Canvas Agent backend package.

The first compatibility release keeps orchestration in ``backend.py`` while
the stable plugin boundaries are exercised. Subsequent internal splits do not
change the manifest or HTTP contract.
"""
